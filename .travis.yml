language: python
python:
    - "2.7"
    - "3.5"
    - "3.6"
install: pip install tox
script:
    - TOXENV=$(tox --listenvs | grep "py${TRAVIS_PYTHON_VERSION/./}-" | tr '\n' ',')
    - export TOXENV
    - tox
